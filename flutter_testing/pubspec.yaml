name: flutter_testing
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  provider: ^6.1.2
  go_router: ^14.2.7

dev_dependencies:
  flutter_test:
    sdk: flutter

  test: ^1.26.2

  flutter_driver:
    sdk: flutter

  integration_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
